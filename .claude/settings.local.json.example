{
  "_说明": "复制为 settings.local.json（已 gitignore）。个人配置：只放你自己的权限与密钥引用，不进 git。",
  "permissions": {
    "allow": [
      "Bash(npm run *)",
      "Bash(docker compose *)"
    ]
  },
  "env": {
    "ZHIPU_API_KEY": "在 deploy/service-agent.env 里配，不要写这里之外的地方"
  }
}
